# 기능 백로그 — 브레인스토밍·평가 기록

오푸스5가 브레인스토밍하고 페이블 5.1이 평가해 구현 계획으로 옮긴다. 라운드마다 아래에 누적한다.
채택된 항목만 구현 대상이다. 기각 항목은 **왜 기각했는지**를 남겨 다음 라운드에서 같은 제안이 다시 올라오지 않게 한다.

## 상태 기호

- `[ ]` 채택, 미착수
- `[~]` 진행 중
- `[x]` 완료(커밋 해시)
- `[-]` 기각 (사유 필수)
- `[?]` 운영자 판단 필요 — 물어볼 때까지 대기

---

# 라운드 1 (2026-09-05)

브레인스토밍 26건 + 제약위반 5건. 아래 판정은 실제 코드 확인을 거쳤다.

## A. 채택 — 즉시 (근거가 확인됐고 리스크가 없다)

### A1. `/tools` 색인을 실제 도구 앵커로 링크 `[ ]` S

`app/tools/page.tsx:86`이 24개 도구 전부를 `/guide/{stage}#tools` 한 곳으로 보낸다.
그런데 `lib/stageToolMeta.ts`의 `TOOL_IDS`와 `StageTools`의 해시 딥링크는 **이미 동작한다**.
색인 페이지만 그걸 안 쓰고, 대신 66~72줄에서 사용자에게 한계를 사과하고 있다.

- href를 `/guide/{stage.slug}#tool-{TOOL_IDS[title]}`로 교체
- 사과 문단 삭제 → 안내 한 줄로 교체
- `app/showcase/page.tsx`의 `/guide/submission#tools` → `#tool-showcase`
- 테스트: `TOOL_IDS`에 없는 제목이 오면 타입 에러로 이미 막힌다. 추가 테스트 불필요.

**평가**: 라운드 1 최고 가성비. 이미 만든 기능이 연결만 안 돼 있는 상태다.

### A2. sitemap `lastModified`를 문서별 날짜로 `[ ]` S

`app/sitemap.ts:8`의 `const lastModified = new Date()`를 40여 URL이 공유한다.
배포마다 31편 전부가 "방금 수정됨"으로 나가면 크롤러가 이 신호를 버린다.
색인 할당량이 빠듯한 지금 특히 손해다.

- 글: `new Date(article.updated)`
- 사례: `new Date(showcase.published)`
- 단계·정적 페이지: 빌드 시각 유지 (실제로 코드와 함께 바뀐다)
- 테스트: 자료실 URL의 lastModified가 서로 다른 값을 갖는지

### A3. Article JSON-LD에 `image` 추가 `[ ]` S

`lib/jsonLd.ts`가 "모르는 필드는 지어내지 않는다"며 `image`를 뺐는데,
`app/articles/[slug]/opengraph-image.tsx`가 생긴 뒤로 이미지는 **아는 값**이 됐다.
원칙과 충돌하지 않는다. 기사 리치결과 자격 조건이다.

- `image`에 `{SITE_URL}/articles/{slug}/opengraph-image`
- 사례 JSON-LD도 같이 (showcase OG 있음)
- 주석의 "지어내지 않는다" 문장은 유지하되 "OG 라우트가 실재하므로 image는 예외"를 덧붙인다

### A4. 자료실 상세에 광고 슬롯 1개 `[ ]` S

검색 유입이 가장 크게 떨어질 31편 롱폼의 수익 면적이 0이다.
`app/articles/[slug]/page.tsx`에 `AdSlot` import 자체가 없다.

- 본문 끝 ~ "이어지는 문서" 사이, `2xl:hidden` (단계 페이지와 동일 패턴)
- **본문 중간 삽입은 하지 않는다.** 애드센스 재검토 중 콘텐츠/광고 비율을 건드리지 않는다.

### A5. 단계 페이지 "함께 읽기" 4편 상한 `[ ]` S

`app/guide/[stage]/page.tsx`의 `relatedArticles.map`에 상한이 없다.
`topic`·`prior-research`가 각 8편이라 설계 문서가 경고한 상한에 이미 닿았다.

- 4편 + "자료실에서 더 보기 → `/articles#{category}`"

### A6. 자료실 인쇄 스타일 `[ ]` S

`app/globals.css`의 `@media print`가 체크리스트용만 있다. 학교 현장은 종이다.

- 목차 nav·이전/다음·"다른 문서" 숨김
- 내부 링크 뒤에 URL 표기
- 표·코드블록 `break-inside: avoid`

### A7. 404를 막다른 길에서 빼기 `[ ]` S

`app/not-found.tsx`가 버튼 두 개뿐. 색인 URL이 51개다.

- `getFeaturedArticles(4)` + `/articles`·`/tools` 링크

## B. 채택 — 검증 후 (설계 판단이 하나씩 붙는다)

### B1. 통계 계산기에 대응표본 t·카이제곱 `[ ]` M

`docs/articles-content-plan.md` §5-A가 통계 글에 검정 선택표를 넣고 계산기로 링크하기로 했는데,
`lib/stats.ts`는 Welch t·상관·회귀 셋뿐이다. 표가 안내한 칸을 계산기가 못 받는다.

- 대응표본 t: 차이의 평균/표준편차 → 기존 `tTestTwoTailedP` 재사용, df = n-1
- 카이제곱 독립성: 2×2부터 r×c까지. p값에 불완전 감마 함수 필요 — `chiSquareP` 추가
- **ANOVA는 이번 라운드 제외.** F분포 p값(불완전 베타)이 추가 수치해석 부담이고,
  중고생 연구에서 3집단 이상은 대응 t·카이제곱보다 드물다. 다음 라운드 재검토.
- `lib/statsVerdict.ts`에 각 검정의 해석 문구 추가 — 기존 "유의하다≠크다" 톤 유지
- 테스트: R/scipy로 검산한 고정값 케이스 최소 6개

### B2. 차트 오차막대 `[ ]` M

`content/articles/14-figure-first.mdx`가 "오차막대를 읽어라"라고 가르치는데
`SimpleChart`로는 그릴 수 없다. 가르치는 것과 주는 도구가 어긋난다.

- `lib/chartGeometry.ts`의 `parsePairs`를 3열(라벨, 값, ±)로 확장 — **3열은 선택**
- bar·line에만. 산점도·원형은 무의미하므로 UI에서 열 자체를 숨긴다
- 축 범위 계산에 오차 상단/하단을 포함해야 잘리지 않는다 (이게 실제 함정)

### B3. 마감일 `.ics` 내보내기 `[ ]` S

`31-finding-calls`의 결론이 "공고를 늦게 봐서 못 낸다"인데
마감이 이 브라우저 localStorage에만 남으면 그 문제를 그대로 재현한다.
서버 없이 알림을 줄 수 있는 유일한 정직한 경로다.

- `lib/ics.ts` 순수 함수 + `PlanExport`의 Blob 다운로드 패턴 재사용
- VALARM 7일 전 1개. 여러 개 넣지 않는다(캘린더 앱마다 처리가 다르다)
- **CRLF 줄바꿈과 75옥텟 폴딩**을 지켜야 한다. 한글 SUMMARY에서 UTF-8 바이트 경계를 쪼개면 깨진다 — 여기가 유일한 난점
- 테스트: 한글 제목 폴딩, 특수문자 이스케이프(쉼표·세미콜론·역슬래시), DTSTART 형식

### B4. 레퍼런스 BibTeX·RIS 내보내기 `[ ]` S

`lib/citations.ts`가 APA·IEEE 문자열만 만든다.
`PriorResearchSearch`로 모은 논문이 사이트 밖으로 못 나가면 도구 체인이 거기서 끊긴다.

- `Reference` 타입 필드가 그대로 매핑된다
- BibTeX 키 생성 규칙(저자성+연도+제목첫단어)과 한글 저자 처리 주의
- 중괄호·앰퍼샌드·퍼센트 이스케이프

### B5. 도구 → 자료실 역링크 `[ ]` M

링크가 한 방향뿐이다. 글→도구는 `articles.test.ts:258`이 검증까지 하는데 반대가 없다.

- `lib/stageToolMeta.ts`에 `TOOL_ARTICLES` 추가
- `/tools` 항목과 도구 카드 하단에 1~2줄
- **테스트 필수**: 존재하지 않는 slug를 막는다. `articles.test.ts`의 링크 검증 패턴 재사용

### B6. 읽기 시간 표시 `[ ]` S

목록에서 FAQ 24문항과 짧은 글이 구분되지 않는다.

- `lib/korean.ts` 글자수 로직 재사용, 한국어 분당 500자 기준
- MDX 문법(링크 URL, 코드블록)을 세면 과대 계상된다 — 본문 정규화 필요

### B7. 자료실 RSS `[ ]` S

`app/feed.xml/route.ts`. 라운드 단위로 글이 계속 늘어난다.

- `updated` 내림차순, 최근 20편
- `app/layout.tsx`에 alternate 링크
- CDATA와 상대→절대 URL 변환

### B8. PWA 매니페스트 `[ ]` S

데이터가 기기에만 있는 앱이므로 "이 기기에 설치"라는 심상이 실제 동작과 일치한다.

- `app/manifest.ts`(Next 규약)만. **서비스워커는 하지 않는다** — 애드센스 심사 중 광고 스크립트를 캐시하면 정책 위반 소지

### B9. FAQ 구조화 데이터 `[ ]` M — 우선순위 낮음

`01-faq.mdx`가 질문 24개인데 FAQPage를 버리고 있다.

- **MDX 본문 파싱만으로는 안 된다.** 취약하고 조용히 깨진다.
- `### 질문` 다음 첫 문단을 뽑는 파서 + **본문과 개수 일치 테스트**로 조용한 실패를 막는다
- 구글 정책상 FAQPage 리치결과는 정부·의료 사이트로 제한됐다 — **표시 효과는 기대하지 말고**, 구조 정확성만 취한다. 그래서 우선순위 낮춤.

## C. 조건부 채택 — 크고 순서가 있다

### C1. 사이트 내 검색 `[ ]` L

31편 + 도구 24개 + 사례 6편. 목록 스캔의 한계를 넘었다.

- 빌드 타임 경량 인덱스: 제목 + summary + keywords만. **본문 제외** — 번들 폭증
- 한국어 부분일치가 핵심이다. 형태소 분석 없이 substring + 초성 검색으로 충분하다
- 헤더에서 여는 모달
- **A1~A7 완료 후 착수.** 작은 개선들이 먼저 나가야 검색이 찾을 대상이 정돈된다.

### C2. 목록 페이지 OG 이미지 `[ ]` M

`/tools`·`/articles`·`/example`이 루트 이미지를 공유해 카톡 공유가 전부 같은 그림이다.
학생 간 공유는 카톡이 절대다수다.

- `lib/ogFontChars.ts`에 문자 추가 → `npm run og-font` 재실행
- **폰트 서브셋 갱신을 잊으면 글자가 두부가 된다.** 체크리스트에 넣는다

### C3. 시리즈 묶기 `[ ]` M

`docs/articles-taxonomy-design.md`가 "연작이 둘 이상 생기면"이라 유보했고, 지금 넘겼다
(논문 읽기 3편, 주제 발굴 3편).

- frontmatter `series` 문자열 + 상세 하단 박스
- **라우트는 만들지 않는다** — 설계 문서의 "얇은 페이지 금지" 유지
- 카테고리 내 prev/next와 시리즈가 어긋나 보이지 않게, 시리즈 박스를 prev/next보다 위에 둔다

### C4. `saveError` 공용 표시 `[ ]` M

`usePersistentState`가 `saveError`를 주는데 대부분의 도구가 무시한다.
iOS 프라이빗 모드에서 20분 입력하고 새로고침 한 번에 전부 잃는다.

- `components/PersistNotice.tsx` 하나 + 입력량 많은 도구부터: 변수 정의표, 레퍼런스, 마감일, 사례 공유 양식
- 문구는 겁주지 않게: "이 브라우저에 저장하지 못했습니다. 시크릿 모드이거나 저장 공간이 가득 찼을 수 있어요. 지금 내용을 복사해 두세요."

### C5. 투고처 목록 확장 `[ ]` M

`docs/articles-content-plan.md` §8이 위치와 형태까지 정해둔 미결 항목이다.

- 항목에 "공고 확인처 URL"과 "대략적 시기(월)"만. **정확한 마감일은 넣지 않는다**
- 설계 문서가 거부한 "분기별 갱신 캘린더"는 만들지 않는다 — 낡으면 해악
- 조사 시간이 대부분이고, 사실 검증이 필요하다

### C6. 비어 있는 사례 슬롯 채우기 `[ ]` M

운영자 파일 입력을 무기한 기다리는 대신, `content/showcase/` 6편을 해당 글에 인용+링크로 붙인다.
`/showcase/{slug}` 링크는 `articles.test.ts:227`이 이미 검증한다.

## D. 기각

### D1. DOI → Crossref 자동 채우기 `[-]`

**기각 사유**: 제안 자체는 좋으나 지금이 아니다. `app/api/search-papers/route.ts`가 이미 있어
기술적으로는 라우트 하나지만, 학생이 붙여넣는 DOI가 서버 로그에 남는다.
`/privacy`의 "서버로 전송되지 않습니다" 문장과 부딪힌다 — 논문 검색은 이미 예외로 명시돼 있지만
인용 도구는 "내가 쓰는 참고문헌 목록"이라 성격이 다르다.
**재상정 조건**: `/privacy`에 예외 범위를 정확히 적고, 운영자가 승인한 뒤.

### D2. 클라이언트에서 Crossref 직접 호출 `[-]`

(D1의 우회로로 떠오를 수 있어 미리 적는다)
**기각 사유**: CORS는 통과하지만 학생 IP가 Crossref에 직접 노출되고 polite pool 이메일을 클라이언트에 박아야 한다.

### D3. 홈 `WebSite` + `SearchAction` 구조화 데이터 `[-]` (부분)

**기각 사유**: `SearchAction`은 실제 검색 URL이 있어야 한다. C1 전에는 거짓말이다.
`Organization`은 `/about`에 이미 운영자 실명이 있어 새로 만드는 실익이 작다. **C1과 함께 재검토.**

### D4. 목록 페이지 `ItemList` JSON-LD `[-]`

**기각 사유**: 구글이 `ItemList`로 리치결과를 주는 건 특정 유형(레시피, 영화 등)뿐이다.
일반 문서 목록에는 효과가 사실상 없다. 코드만 늘어난다.

### D5. 서비스워커 오프라인 캐시 `[-]`

**기각 사유**: 애드센스 심사 중 광고 스크립트 캐시는 정책 위반 소지. 매니페스트(B8)까지만.

## E. 운영자 판단 대기 `[?]`

물어볼 때까지 진행하지 않는다.

1. **헤더에 "자료실" 추가** — `SiteNav.tsx` 주석이 375px에서 가용폭을 이미 다 썼다고 **실측**으로 적어 놨다.
   4번째 항목을 넣으면 nav-scroll이 받아주지만 좁은 화면에서 스크롤이 생긴다.
   "예시"를 빼고 "자료실"을 넣을지, 스크롤을 감수할지가 운영자 판단이다.
2. **홈에 사례 섹션** — 사례 6편이 홈에서 링크되지 않는다. 홈 길이를 늘릴지.
3. **`NEXT_PUBLIC_ADSENSE_CLIENT_ID`** — 미설정이면 광고 0개를 위해 전 페이지가 스크립트를 받는다.
4. **"지니어스 클럽" 팀명 복원** — ARIA 사례에서 익명화하며 제거했다.
5. **익명 집계 통계 롤아웃** — `docs/anonymous-stats-design.md`가 "애드센스 재검토 결과 뒤"로 순서를 정해뒀다.

## F. 제약 위반 확인 (재제안 금지)

무로그인·서버 상태 없음 원칙과 충돌한다. 오푸스5가 정확히 분류했다.

- 기기 간 동기화 / 계정 (Supabase) — `/privacy` 약속과 충돌
- 사례 자동 접수 — 미성년자 개인정보 처리
- 댓글·질문 게시판 — UGC 모더레이션, 심사 중 특히 위험
- 논문 전문 PDF 프록시 — 저작권
- 익명 집계 — 설계는 끝났고 순서만 대기 (E5)

---

## 다음 라운드에 넘기는 메모

오푸스5가 다음에 다룰 때 **이미 판정된 위 항목은 다시 올리지 말 것.**
아직 훑지 않은 영역:

- 6단계 가이드 본문 자체의 품질 (도구·자료실만 봤다)
- `/example` 페이지 (예시 보고서)
- `/activity` 기록 화면의 실사용 흐름
- 모바일 전용 문제 (지금까지 데스크톱 관점이 많았다)
- 지도교사 관점 (학생 관점만 봤다)
- 검색 유입 키워드 → 없는 콘텐츠의 공백

---

# 라운드 2 (2026-09-05)

라운드 1이 안 본 6개 영역(가이드 본문·/example·/activity·모바일·지도교사·콘텐츠 공백).
브레인스토밍 20건. 라운드 1 판정과 중복 없음을 확인했다.

## 라운드 1 판정 정정

브레인스토밍 중 라운드 1의 사실 오류 두 건이 나왔다. 실제 코드로 확인했고 맞다.

- **A6 수정**: `app/globals.css`에 `print` 문자열이 **0개**다. 인쇄 스타일은 전부
  `components/PrintableChecklist.tsx`의 Tailwind `print:` 유틸리티로만 있다.
  A6 구현 시 "기존 블록에 추가"가 아니라 `@media print` 블록을 새로 만들어야 한다.
- **주석 오류**: `lib/stageToolMeta.ts:5` 주석이 "21개 도구"라고 하는데 실제 24개다.
  A1 작업 시 함께 고친다.

## G. 채택 — 즉시 (라운드 2)

### G1. `/example`의 도구 이름을 앵커 링크로 `[ ]` S

`/example`이 도구 이름을 8번 언급하는데(168·176·269·288·341·345·349줄) 전부 따옴표 텍스트다.
A1과 같은 판단인데 **발신처가 다르다**. `/tools`는 색인이고 `/example`은
"방금 그 산출물을 봤다"는 상태라 전환 의도가 훨씬 높다.

- `/guide/{stage}#tool-{TOOL_IDS[title]}` 링크
- `TOOL_IDS`가 도구 제목 유니온 타입이라 오타는 컴파일에서 잡힌다
- **A1과 같은 커밋에서 처리한다.** 같은 메커니즘이고 따로 나눌 이유가 없다

### G2. `05-writing.mdx:68`의 낡은 문장 정정 `[ ]` S

"계산기가 주지 않는다면 d를 직접 구해 적으세요"라고 쓰여 있는데
`StatsCalculator.tsx:39`의 `groupSummary`가 `d = ...`를 **이미 출력한다**.
도구를 불신하게 만드는 문장이다.

- "계산기가 d를 함께 출력하니 결과 문장에 그대로 옮기세요"로 교체
- 한 문장이지만 손실이 크다

### G3. `/activity` 빈 상태 안내 `[ ]` S

기록이 0건일 때 `ActivityHeatmap`은 회색 칸 84개와 "총 0건"만 그린다.
헤더 3링크 중 하나라 첫 방문자가 반드시 밟는데, 그 상태에서 빈 격자는 "고장난 페이지"다.
기록을 만드는 주체(체크리스트·자가검증·윤리 점검)가 이 페이지 어디에도 안 적혀 있다.

- `totalCount === 0`이면 격자 대신 문단 하나 + `/guide` 링크
- 기록이 있으면 지금 화면 그대로

### G4. `/activity`에서 단계로 되돌아가는 링크 `[ ]` S

`StageDurations`가 `slug`를 prop으로 받고도 쓰지 않는다.
"3단계가 12일 걸렸네"를 본 다음 갈 곳이 없다. 나가는 링크가 `/guide/print` 하나뿐이다.

- 단계 제목을 `/guide/{slug}` 링크로. 한 줄이다

### G5. `/activity`에 "마지막 활동 이후 N일" `[ ]` S

한 학기 연구의 가장 흔한 실패는 중단인데, 이 페이지가 회고 재료만 주고 판단 재료를 안 준다.

- `lib/activity.ts`에 `getLastActivity` 추가
- "마지막 기록: 6월 2일 (11일 전) · 진행 중: 4단계"
- **겁주는 톤 금지.** `guide/page.tsx`의 "건너뛰거나 보류해도 괜찮습니다" 기조를 지킨다

### G6. 넓은 표에 가로 스크롤 단서 `[ ]` S

`app/guide/[stage]/page.tsx:134-142`가 표를 `overflow-x-auto`+`min-w-max`로 감싸며
주석에 "좁은 화면에서 실제로 가로 스크롤된다"고 의도까지 적어 뒀는데,
**스크롤이 가능하다는 시각 단서가 없다.** 6열 표에서 학생은 표가 잘린 줄 모른다.
표는 6단계 가이드의 주된 정보 형식이다.

- 오른쪽 페이드 마스크(CSS만) 또는 표 위 `text-xs` 한 줄
- `/example`의 변수 표도 같은 처리

### G7. 변수 정의표 행의 모바일 레이아웃 `[ ]` S

`VariableTableBuilder.tsx:59`가 `flex flex-wrap`에 `flex-1` 두 개 + select + 삭제 버튼.
375px(가용 약 303px)에서 랩되는데 삭제 버튼이 어느 행 것인지 안 붙고, 행 구분선도 없다.
3단계 핵심 산출물이고 학생이 4~5행을 채우는 도구다.

- `sm:` 미만에서 행을 카드로. 이상은 지금 그대로

## H. 채택 — 검증 후 (라운드 2)

### H1. 표본 크기 계산기에 '두 집단 평균 비교' 모드 `[ ]` M

**라운드 2에서 가장 아픈 발견이다.** `03-methodology.mdx:105`가 학생을 계산기로 보내는데
`SampleSizeCalculator.tsx:60`이 "이건 설문·비율용이라 못 합니다"라고 답한다.
그리고 `app/example/page.tsx:238`이 그 실패를 예시 연구의 **줄거리로 박아뒀다** —
"사이트의 표본 크기 계산기는 두 집단 평균 비교에는 쓸 수 없었습니다. 선생님께 여쭤보니…".

사이트가 자기 도구의 무능을 모범 사례의 서사로 쓰고 있다. 이건 고쳐야 한다.

- 탭 추가. 입력: 효과크기 d(또는 두 평균+SD), 검정력, 유의수준
- 정규근사 후 교과서 보정 → d=0.5, 검정력 80%에서 집단당 64가 나온다
- 기존 `Z_TABLE` 재사용. t분포 불필요
- **연쇄 수정 필수**: `03-methodology.mdx:105`, `04-data-collection.mdx:22`,
  `app/example/page.tsx:238-245`를 "계산기로 확인했더니 64명"으로 교체.
  `SampleSizeCalculator.tsx:60`의 사과 문단도 삭제
- 테스트: d=0.2/0.5/0.8 × 검정력 80%/90%의 교과서 값과 대조

### H2. `welchTTest`에 평균 차이 신뢰구간 `[ ]` M

`app/example/page.tsx:279`가 모범 결과 문장에 "95% CI [0.6, 4.2]"를 넣고
283-286줄에서 "신뢰구간이 넓다"를 해석의 핵심으로 삼는다.
그런데 `lib/stats.ts`에 CI를 만드는 함수가 없다. 예시를 따라 하려면 사이트 밖에서 손계산해야 한다.
`05-writing.mdx:62-66`의 "함께 적을 것" 표에도 신뢰구간 행이 없어 본문과 예시가 다른 기준을 가르친다.

- `welchTTest` 반환에 `ciLow`/`ciHigh`. t 임계값은 `tTestTwoTailedP`의 이분탐색 역함수로 얻는다
- Welch–Satterthwaite df를 그대로 쓴다
- 복사 문자열과 `05-writing.mdx` 표에 신뢰구간 행 추가
- **B1과 같은 작업 단위**로 묶는다. 둘 다 `lib/stats.ts`를 건드린다

### H3. 검색 기록표 — `PriorResearchSearch`에 "기록에 남기기" `[ ]` M

`02-prior-research.mdx:50-55`가 "검색할 때마다 날짜·사이트·키워드·건수를 표로 남기세요"라고
시키고 예시 표까지 주는데, 2단계 도구 둘 중 어디에도 로그를 남길 곳이 없다.
`PriorResearchSearch`는 검색을 실제로 하면서 그 데이터를 버린다.

- 버튼 하나. 키워드·건수·날짜는 컴포넌트 안에 이미 있는 값이라 자동으로 채워진다
- 사용자는 메모만 적는다
- `usePersistentState` + 마크다운 표 복사(`VariableTableBuilder` 패턴)

### H4. 데이터 정리 기록표 도구 `[ ]` M

`04-data-collection.mdx:102-109`가 "몇 건 중 몇 건을 무슨 이유로 뺐는지" 표를 요구하고
"이 표는 논문에 그대로 들어갑니다"라고 못 박는다. 4단계 도구 셋 중 이걸 만들 곳이 없다.
같은 단계 32줄에서 이상치 기준 항목의 도구 칸이 비어 있는 것이 같은 공백이다.

- `VariableTableBuilder` 골격 복제. 단계명/건수/제외 사유 3열
- **이상치 기준을 수집 전에 적는 칸을 맨 위에** — "결과를 본 뒤에 빼면 안 된다"를 UI로 강제
- 재현가능성의 핵심 산출물이다

### H5. 자가검증 완료 판정 `[ ]` M

`app/guide/page.tsx:31`이 "다음 단계로 가는 기준"을 광고하는데 화면에 구현이 없다.
각 단계 끝의 '다음 단계' 절이 전제 조건을 문장으로 걸지만,
하단 다음 버튼은 그 조건과 무관하게 항상 같고 바로 위 `ReflectionBox`의 답과 연결되지 않는다.

- 자가검증 카드 하단에 판정 한 줄. 답이 다 차면 "넘어갈 준비가 됐습니다", 아니면 남은 질문
- **게이트가 아니라 표시로만.** 건너뛰기를 막지 않는다 — "보류해도 괜찮습니다" 기조 유지
- `ReflectionBox`가 이미 `logActivity`까지 하고 있어 데이터는 있다

### H6. 모바일 단계 점프 바 `[ ]` M

`StageRail.tsx:137`이 `hidden lg:block`이라 **375px에서 목차·도구 개수·진행도가 전부 사라진다.**
5단계는 도구가 9개인데 첫 도구가 `StatsCalculator`(300줄+)라,
인용 형식 도구를 쓰러 온 학생은 본문 전체와 통계 계산기를 지나쳐야 8번째 제목에 닿는다.
도구 24개가 이 사이트의 차별점인데 모바일에서 그 존재 자체가 안 보인다.

- `lg:hidden sticky` 얇은 바. "도구 9 · 체크리스트 · 자가검증" 앵커 3개
- `toolCount`/`selfCheckCount`는 `app/guide/[stage]/page.tsx:80-82`가 이미 계산해 넘긴다
- **sticky 높이가 헤더와 겹치지 않게** — 헤더가 이미 sticky다. `top` 값 실측 필요

### H7. `/example` 상단 단계 앵커 칩 `[ ]` S

382줄 단일 스크롤에 목차가 없다. `StageRail`도 `lg:block`이라 모바일에 없다.
이 페이지는 "내 단계 산출물 하나 보러" 재방문하는 페이지지 처음부터 읽는 페이지가 아니다.

- 상단에 6단계 앵커 칩 줄(`nav-scroll` 재사용) + `StageHeading`에 `id`/`scroll-mt-24`
- G6·H6과 함께 "모바일 라운드"로 묶는다

## I. 조건부 채택 (라운드 2)

### I1. `/teacher` 지도교사 안내 페이지 `[ ]` M

전 코드베이스에서 교사를 향한 서술 5곳이 전부 학생에게 "선생님께 확인받으라"고 시키는 문장이다.
교사가 이 사이트를 **자기 도구로** 쓸 진입점이 0개다.
그런데 6단계 구조·체크리스트·자가검증은 그대로 수업 운영 골격이다.

- (a) 6단계 × 한 학기 16주 운영표 — `estimatedWeeks`가 6단계에 다 있다
- (b) 단계별 학생 제출 산출물(= 체크리스트 항목)
- (c) 반에 나눠줄 것: `/guide/print`, `/example`, 자료실 관련 편
- **학급 관리 기능은 만들지 않는다.** 무로그인 원칙
- 학생 한 명보다 교사 한 명이 데려오는 폭이 훨씬 넓다

### I2. 단계별 평가 루브릭 `[ ]` M

교사가 검색하는 물건이 "루브릭"이다. 재료는 이미 본문에 흩어져 있다 — 재조립이지 창작이 아니다.

- 3수준(미흡/보통/우수) × 6단계
- **새로 지어내지 말 것.** 기존 `selfCheck` + 각 단계 '흔히 하는 실수' 절을 미흡 수준의 기술로 옮긴다
- 인쇄 가능하게. I1 안에 둔다

### I3. `/example`에 비실험 연구 사례 `[ ]` L

`03-methodology.mdx:38-43`이 설계 유형 넷을 가르치고 `01-topic.mdx:31`이
"실험은 방법의 한 종류일 뿐"이라고 못 박는데, `/example` 382줄 전체가 실험 한 편이다.
장비 없는 학교가 다수다. 사이트가 세 군데서 "실험 아니어도 된다"고 말하면서 예시는 실험만 준다.

- 탭 둘: 실험형 / 공개데이터 자료분석형
- 두 번째는 `03-methodology.mdx:60`의 열섬 예시를 확장. 출처·필터·전처리 기록, 상관/회귀, 인과 주장 불가
- **분량이 커지면 `app/example/[kind]/page.tsx`로 분리.** 지금도 382줄이다
- H1·H2 이후에 착수한다 — 그 사례의 통계를 도구가 지원해야 말이 된다

### I4. 콘텐츠 공백 3편 `[ ]` M × 3

세 편 모두 **사이트가 전제하면서 지원하지 않는** 지점이다. 우선순위 순:

1. **"학교 수행평가·세특용 탐구보고서 쓰는 법"** — `app/guide/page.tsx:40`이
   "수행평가라면 5단계에서 끝내도 된다"고 인정하는데 31편 중 이 경로를 다루는 글이 0편이다.
   투고가 아니라 수행평가 때문에 오는 학생이 다수고, 검색량이 자릿수 차이다.
   기존 `05-writing`을 학교 맥락으로 번역하는 글이라 사실 조사 부담이 적다.
2. **"동의서를 실제로 어떻게 만드나"** — 동의 절차가 네 군데서 필수인데
   무엇이 들어가야 하는지 알려주는 문서도 도구도 없다.
   `04-data-collection.mdx:36`의 도구 칸이 비어 있는 것이 증거다. 없으면 연구가 시작조차 못 한다.
3. **"지도교사·멘토를 어떻게 구하고 무엇을 부탁하나"** — 사이트가 멘토를 네 번 전제하고
   `01-topic.mdx:66`은 없으면 "주제를 옮기라"고 한다. 막다른 길인데 지원 문서가 없다.
   무엇은 부탁하면 안 되는지(대신 써주기)까지 다뤄야 윤리 절과 맞물린다.

## J. 기각 (라운드 2)

### J1. ANOVA — B1에서 이미 유보 `[-]`

반복 확인. F분포 p값 부담 대비 사용 빈도가 낮다. **다음 라운드 재검토 대상으로만 유지.**

### J2. 교사용 반 진행도 대시보드 `[-]`

**기각 사유**: 학생 데이터가 각자 기기 localStorage에만 있다. 서버 없이 불가능.
I1·I2는 이 선을 넘지 않게 "인쇄물·운영표"로만 설계했다.

### J3. 동의서 PDF 자동 생성·서명 수집 `[-]`

**기각 사유**: 미성년자 개인정보 처리. I4-2는 "문안 예시를 읽고 학생이 직접 만든다"까지만.

### J4. `/activity` 기록을 교사에게 제출하는 공유 링크 `[-]`

**기각 사유**: 서버 상태 필요. `/privacy`의 "서버로 전송되지 않습니다"와 충돌.

---

## 실행 순서 (라운드 1+2 통합)

작업 단위로 묶었다. 같은 파일을 건드리는 것끼리 붙였다.

| 묶음 | 항목 | 성격 |
| --- | --- | --- |
| **1. 앵커 연결** | A1 + G1 + 주석 정정(24개) | 이미 만든 기능을 연결만 |
| **2. SEO 소품** | A2 A3 A4 A5 A7 B7 B8 | 각각 독립, 리스크 없음 |
| **3. 모바일** | G6 G7 H6 H7 + A6 | 375px 실측 필요 |
| **4. /activity** | G3 G4 G5 | 한 파일군 |
| **5. 통계** | H1 H2 B1 + G2 + 연쇄 문구 수정 | `lib/stats.ts` 집중, 테스트 무겁다 |
| **6. 도구 신설** | H3 H4 B2 B3 B4 | 서로 독립 |
| **7. 링크·구조** | B5 B6 C3 C6 C4 | |
| **8. 교사** | I1 I2 | 새 라우트 |
| **9. 콘텐츠** | I4(3편) C5 | 사실 검증 필요 |
| **10. 큰 것** | C1 C2 I3 B9 | 앞이 끝난 뒤 |

**묶음 1과 2가 먼저다.** 배포 리스크가 0에 가깝고 색인·수익에 바로 닿는다.
묶음 5는 테스트를 먼저 쓴다 — 통계는 조용히 틀리면 학생 논문이 틀린다.

## 다음 라운드에 넘기는 메모

라운드 1·2에서 판정된 55건은 다시 올리지 말 것. 아직 안 본 영역:

- `app/api/search-papers` 실패 모드 — S2 키 없음, 레이트리밋, 세 소스 결과 품질 차이
- 접근성 재점검 — 지난 라운드 대비 회귀가 있는지, 키보드만으로 도구 24개를 다 쓸 수 있는지
- `content/showcase/` 6편의 편집 품질과 사례 축의 성장 전략
- 번들·로딩 — 도구가 늘어난 뒤의 라우트별 JS
- 오류 처리 — 붙여넣기 데이터가 이상할 때 각 계산기가 무엇을 말하는지 (지난 라운드에 3건 나왔다)
- 국제화·다른 학년대 — 중학생에게 이 문장들이 읽히는지

---

# 라운드 3 (2026-09-05)

검색 API·오류 처리·접근성·번들·사례 편집·중학생 가독성. 브레인스토밍 22건.
**이번 라운드는 성격이 다르다 — 기능 제안이 아니라 결함 보고가 다수다.**
치명 3건은 직접 코드로 재현 경로를 확인했다. 전부 사실이다.

번들은 새 제안 없음. 24개 도구가 전부 `next/dynamic`이고 접힌 도구는 렌더조차 안 되며,
MDX는 빌드 타임, OG 폰트는 클라이언트에 도달하지 않는다. 앱 청크 최대 29KB. 이미 끝나 있다.

## K. 치명 — 최우선 (조용한 데이터 손실·조용히 틀린 답)

**이 셋이 다른 모든 항목보다 먼저다.** 서로 다른 파일이라 병렬 가능하다.

### K1. 백업 복원이 실패하면 기존 기록을 전부 잃는다 `[ ]` S

`PlanBackup.tsx:113-114`가 **먼저 전부 지우고 나서 쓴다.**

```
for (const key of listRecordKeys()) window.localStorage.removeItem(key);
for (const [key, value] of entries) window.localStorage.setItem(key, value);
```

쓰기 루프 중간에 예외가 나면(용량 초과, 프라이빗 모드) `:118`의 `catch`가
`fail(GENERIC_ERROR)`를 부른다. 그 문구는 **"파일을 읽지 못했습니다"** — 아무 일도
없었던 것처럼 말한다. 실제로는 한 학기 기록이 지워지고 일부만 복원된 상태다.
`:117`의 `reload()`도 실행되지 않아 화면은 옛 상태로 남는다. 사용자는 모른다.

- 지우기 전에 전체 스냅샷을 메모리에 뜬다
- 실패 시 스냅샷으로 롤백
- 롤백조차 실패하면 **다른 문구**를 쓴다: "복원에 실패했고 기존 기록이 손상됐을 수 있습니다"
- `GENERIC_ERROR` 하나로 파싱 실패와 쓰기 실패를 덮으면 안 된다
- 테스트: `setItem`이 n번째에 throw하는 모의 저장소로 롤백을 검증

### K2. "저장됨"이라고 말하고 저장하지 않는다 `[ ]` S

`lib/citations.ts:98-106`의 `addReference`가 `setItem` 실패를 삼키고
주석에 "저장 실패해도 next는 그대로 반환"이라고 **적어 뒀다.**
호출부 둘 다 성공을 전제한다 — `CitationFormatter.tsx:64`는 `setSaved(true)`,
`PriorResearchSearch.tsx:46`은 버튼을 "저장됨"으로 바꾼다.
논문 10편을 저장하고 새로고침하면 전부 없다. `removeReference`도 같다.

- `addReference`가 `{ refs, saved: boolean }`을 반환
- 두 호출부가 실패 시 `PERSIST_ERROR_MESSAGE` 표시
- **C4와는 다른 저장 경로다.** `citations.ts`는 `usePersistentState`를 쓰지 않는다 — 따로 고쳐야 한다

### K3. 상관·회귀에서 X·Y 쌍이 조용히 어긋난다 `[ ]` M

`StatsCalculator.tsx:146`은 `a.length !== b.length`만 본다. **개수가 같기만 하면 통과한다.**
그런데 `lib/stats.ts:296`이 숫자로 못 읽은 토큰을 **위치 정보 없이** 걸러낸다.

재현: 엑셀에서 두 열을 따로 붙여넣는데 X의 3행과 Y의 7행이 각각 비었다
→ 둘 다 길이 N-1 → 검사 통과 → **4행부터 전부 한 칸씩 밀린 쌍으로 상관계수가 계산된다.**

더 나쁘다: 빈 셀은 `\s+`에 흡수돼 `droppedCount`에도 안 잡히므로 경고조차 안 뜬다.
그리고 `lib/chartGeometry.ts:8-11`은 **정확히 이 문제를 인지하고** 쌍 단위로 파싱한다.
계산기만 안 한다.

- 상관·회귀 모드에서 `parsePairs`와 같은 **인덱스 보존 파싱**을 쓴다
- 빈 줄을 값으로 세고, 어느 행이 비었는지 알린다
- 최소한 "3번째 값을 읽지 못해 이후 쌍이 밀렸습니다"를 말해야 한다
- 테스트: 중간에 빈 셀이 낀 두 열 붙여넣기가 오답 대신 오류를 내는지

## L. 채택 — 높음 (라운드 3)

### L1. 추첨기가 요청 인원을 말없이 깎아 전수를 돌려준다 `[ ]` S

`RandomSampler.tsx:29` — `Math.min(Number(count) || 0, items.length)`.
명단 20명에 30명을 요청하면 20이 되어 **명단 전체를 섞어 그대로 돌려준다.**
화면에는 무작위 표본처럼 보이고 경고가 없다.
무작위 표집을 가르치는 도구가 전수조사를 표본이라고 내놓는다.
`Number("3.7")`도 `slice(0, 3.7)`로 조용히 3명이 된다.

- `n > items.length`면 "명단이 20명이라 20명 전원이 뽑혔습니다 — 이건 표본이 아니라 전수입니다"
- 소수 입력은 반올림 사실을 알린다

### L2. 검색 폴백이 필요한 바로 그 상황에서 도달 불가능하다 `[ ]` S

서버는 소스마다 8초를 준다(`route.ts:25`). 순차 3단 + 초록 보강이면 최악 32초.
클라이언트는 10초에 끊는다(`PriorResearchSearch.tsx:7`).

S2가 **타임아웃**나면 — 폴백을 만든 바로 그 이유 — 8초를 쓰고 남은 2초에
Crossref와 OpenAlex를 끝내야 한다. 사실상 불가능하다.
학생은 `ERROR_MESSAGES.timeout`만 본다. Crossref 폴백은 S2가 **빨리** 429를 줄 때만 살아 있다.

- 소스별 타임아웃 8초 → 3.5초 (3단 합 약 10.5초), 클라이언트 10초 → 15초
- 또는 남은 예산을 계산해 각 단계에 나눠 준다 — 이쪽이 정확하다
- L3과 같은 커밋

### L3. `maxDuration`이 없다 `[ ]` S

`app/api/search-papers/route.ts` 전체에 `export const maxDuration`이 없다.
Vercel 기본 10초를 넘기면 플랫폼 504가 나가고 `NextResponse.json`은 실행조차 안 된다 —
`searchChain.ts:113-118`의 한국어 오류 문구가 **사용자에게 절대 닿지 않는 경로**가 있다.
`SUCCESS_HEADERS`의 CDN 캐시도 안 붙는다.

- L2와 함께 처리. 예산을 10초 안에 맞추거나 `maxDuration`을 명시한다
- 어느 쪽이든 지금은 상한이 코드 어디에도 적혀 있지 않다

### L4. 히트맵이 화면낭독기에 84칸을 숫자 하나로 읽어준다 `[ ]` S

`ActivityHeatmap.tsx:76-83` — 각 칸이 `role` 없는 맨 `<div>`에 `aria-label`만 있다.
ARIA 규칙상 `role=generic`에는 접근 가능한 이름을 붙일 수 없어 브라우저가 버린다.
`title`도 낭독기 대체수단이 못 된다. 결국 "총 N건"만 남는다.
`:65`의 주석은 컨테이너에 `role="img"`를 안 쓰는 이유를 적었지만
**칸에 역할을 주는 문제는 다루지 않았다.**

- 칸에 `role="img"`를 주거나, 격자 아래에 `sr-only` 요약 목록
- G3(빈 상태)과 같은 파일이므로 함께 처리한다

### L5. `aria-hidden` 안에 탭으로 들어갈 수 있는 링크 `[ ]` S

`ProgressOverview.tsx:59`가 `<section>`에 `aria-hidden={!hydrated || undefined}`를 걸고
그 안 `:68`에 포커스 가능한 `<Link href="/guide/print">`가 있다.
키보드 사용자가 보조기술에 숨겨진 영역으로 탭 이동한다 — axe `aria-hidden-focus` 위반.
의도(레이아웃 이동 방지)는 링크를 숨길 필요가 없다.

- `aria-hidden` 대신 `visibility: hidden` 또는 `inert`

### L6. 표본 크기 계산기가 오차범위 상한 없이 "1명"을 답한다 `[ ]` S

`SampleSizeCalculator.tsx:27,50`. 조건이 `e > 0`뿐이다.
50을 넣으면 **4명**, 500을 넣으면 **1명**이 나온다.
`:12-15`의 `UNREALISTIC_N`은 e가 너무 **작을** 때만 잡고 클 때는 방어가 없다.
"몇 명한테 돌려야 하나"에 1명이라고 답하는 화면이다.

- `e`에 상한(20%)을 두고 넘으면 "오차범위를 퍼센트로 넣었는지 확인하세요"
- 아래쪽 경고와 대칭을 맞춘다
- **H1과 같은 파일이므로 함께 처리한다**

### L7. `usePersistentState` 쓰기 디바운스 `[ ]` M

`lib/usePersistentState.ts:66-67`이 **키 입력마다** 값 전체를 `JSON.stringify` → `setItem` 한다.
디바운스도, 어떤 텍스트영역에도 `maxLength`도 없다.
보고서 전문을 `LengthChecker`·`SpeechTimer`에 붙여넣고 편집하면 매 글자마다 동기 직렬화가 돈다.
게다가 `SpeechTimer.tsx:9`는 `saveError`를 구조분해조차 하지 않아 용량 초과 시 화면에 표시가 없다.

- 쓰기만 200~300ms 디바운스. 화면 값은 즉시
- **호출부 15곳을 안 고쳐도 된다** — 이게 이 수정의 장점
- 언마운트 시 flush 필수. 안 하면 마지막 입력을 잃는다 (이 수정이 새 버그를 만드는 지점)
- C4와 같은 훅이므로 함께 처리한다

## M. 채택 — 보통 (라운드 3)

### M1. 결과를 뽑는 도구 둘이 낭독기에 말이 없다 `[ ]` S

`TopicIdeaGenerator.tsx:66`, `PresentationQuestionBank.tsx:71`이 버튼을 누르면
`<ul>`을 그리는데 라이브 영역이 없다. 두 파일의 유일한 `aria-live`는 **복사 실패**만 담당한다.
같은 저장소의 `RandomSampler.tsx:101`, `ImradChecker.tsx:146`은 올바른 패턴을 이미 쓴다.

### M2. 오류가 어떤 입력의 것인지 연결돼 있지 않다 `[ ]` S

`aria-describedby`·`aria-invalid`가 저장소 전체에 **0회**다.
오류는 `role="alert"`로 한 번 읽히고 끝이라, 낭독기 사용자가 칸으로 돌아가면 무엇이 잘못됐는지 모른다.

- 대상: `DeadlineTracker` `StatsCalculator` `PriorResearchSearch` `SampleSizeCalculator`
- **오류 문구가 이미 다 있으므로 배선만 하면 된다**

### M3. 통계 도우미 아코디언에 `aria-expanded`가 없다 `[ ]` S

`components/stats/GuideBlock.tsx:29-36` — 저장소에서 유일하게 `<details>`가 아닌
손수 만든 펼침 버튼인데 `aria-expanded`도 `aria-controls`도 없다.
상태가 "접기 ▲ / 펼치기 ▼" 글자로만 전달된다.
게다가 `:97-100`의 검정 추천 결과가 라이브 영역 없이 나타난다 —
**어떤 검정을 쓰라는 핵심 산출물이 조용하다.**

### M4. 사례 6편의 정렬이 파일시스템 순서에 달려 있다 `[ ]` S

`lib/showcase.ts:51`이 `published`로 정렬하는데 6편 전부 `2026-09-05`다. 완전 동점이다.
게다가 파일명 접두사가 **`01-`이 두 개**라 파일명 규약도 이미 깨져 있다.
새 사례 하나를 추가하면 여섯 편이 통째로 밀려날 수 있다.

- 동점 시 2차 정렬 키(slug)를 넣어 **결정적으로** 만든다 — 이게 먼저다
- 파일명 접두사 재부여는 그다음

### M5. 사례 축이 한쪽으로 완전히 쏠려 있다 `[ ]` M

6편 전부 고등학생, 6편 전부 생명·의학이다.
`lib/showcase.ts:14`의 타입은 `"중학생"`을 허용하는데 실물이 0편이다 — **중**·고등학생 사이트인데.
`03-methodology.mdx:36-43`이 설계 유형 넷을 가르치는데 **설문조사 사례가 0편**이다.
실험실 없는 학교가 가장 먼저 고르는 설계이고 `02-survey-questions.mdx`가 통째로 다루는 주제인데도.
난도도 전부 상단이라(EDC 커플링, 자작 EEG 계측증폭기, EfficientNetB4, 룽게-쿠타 4차)
"나도 하겠다"는 사례가 없다.

- 다음 수집 우선순위를 **설문조사형 1편 + 중학생 1편**으로 못 박는다
- `ResearchShowcaseForm`의 안내에 어떤 사례가 부족한지 적는다
- I3(비실험 `/example`)과 같은 문제의 다른 얼굴이다

### M6. 사례 절 구조가 제각각이고 절반이 막다른 길 `[ ]` M

6편 공통 절은 `## 다음 사람에게` 하나뿐이다.
`## 함께 볼 것`은 3편에만 있어 **절반이 나가는 링크 없는 막다른 길**이다.
한계 절은 이름이 셋이고 `01-trem2`는 아예 없다. 첫 절도 둘로 갈린다.

- 고정 골격: 질문 / 방법 / 결과 / 막혔던 지점 / 스스로 밝힌 한계 / 다음 사람에게 / 함께 볼 것
- 서사형 2편은 중간만 자유롭게 두고 앞뒤를 맞춘다
- `## 함께 볼 것`은 6편 전부 필수

### M7. 사례가 화자를 섞어 쓰고 한 편은 파일 안에서 바뀐다 `[ ]` M

1인칭 당사자·3인칭 팀·3인칭 저자 셋이 공존한다.
`01-lysozyme`은 한 파일 안에서 뒤집힌다 — `:17-27`은 1인칭인데 `:43`부터 "보고서는…"으로 넘어간다.
`03-sex-difference-adr:22`는 "이 탐구가 **교과서적으로 잘한 부분이** 이 단계입니다" —
학생 사례 안에서 사이트가 학생을 채점한다.

- 편집된 3인칭으로 통일. 편집자 논평은 별도 콜아웃이나 "다음 사람에게"로만
- M6과 같은 편집 작업에서 함께 처리한다

### M8. 익명화가 조합으로 뚫리는 지점 두 곳 `[ ]` S

실명·학교명·팀명은 전부 없다. 다만 조합이 좁힌다.

- `03-sex-difference-adr.mdx:14` "여름방학에 대학에서 여는 고교생 대상 약학 프로그램" —
  명부가 있는 소수 프로그램이고, `:28`의 정확한 출처 + 12년 구간 + `:30`의 t·p·CI와 합쳐지면
  원본 보고서를 특정할 수 있다
- `01-lysozyme:49` "대학 실험실에 있는 지인에게 EDC를 빌릴 수 있게 되어"

**교육적으로 필요 없는 경로 정보만 뭉갠다.** 통계값과 방법은 그대로 둔다 — 그게 배울 내용이다.
프로그램 참여 경위와 시약 조달 경로만 일반화한다.
미성년자 사례라 이 기준선을 `docs/`에 남길 가치가 있다.

### M9. 정의 없이 쓰이는 핵심 용어 7개 `[ ]` M

**그중 하나는 "베껴 쓰라"고 주는 템플릿 안에 있다.**

| 용어 | 위치 | 문제 |
| --- | --- | --- |
| 선행연구 | `02-prior-research.mdx` 14회 | 사이트 최다 빈도 미정의어 |
| 표집·편의 표집 | `30-limitations.mdx:49` | **학생더러 그대로 베끼라는 한계 서술 템플릿 안**. 중1이 파싱 못 하는 단어 넷이 든 문장을 복사하게 시킨다 |
| 효과 크기 | `26-small-sample.mdx` 4회 | 정의를 다른 글로 미룬다 |
| M·SD·n·t(30)·d | `05-writing.mdx:53` | APA 결과 문장에 기호 다섯이 풀이 없이 |
| 자유도 | `03-statistics.mdx:24` | 표에 맨몸으로 |
| MeSH | `11-pubmed-mesh.mdx` 22회 | 약자 원말이 한 번도 안 나온다 |
| 재현가능성 | `03-methodology.mdx:109` → `05-writing.mdx:102` | 쓰고 나서 두 단계 뒤에 정의한다 |

- 첫 등장에 괄호 한 줄 풀이
- **모범이 이미 있다** — `03-statistics.mdx:53`의 p값 설명이 정확히 그 등록기다. 복제하면 된다
- 새 용어집을 만들지 않는다. 이미 `lib/site.ts`에 논문용어사전이 있고 상시 노출된다.
  그런데 본문에서 링크하는 곳은 `05-reading-english-papers.mdx:105` **단 한 곳**이다.
  어려운 용어가 몰린 문서에는 없다 — 첫 등장에 딥링크를 붙이는 편집으로 한정한다

### M10. 같은 개념을 다른 이름으로 부른다 `[ ]` S

`03-methodology.mdx:100`은 "영가설(H0)"을 가르치는데
`03-sex-difference-adr.mdx:24`는 "귀무가설"을 쓴다.
가이드를 읽고 사례로 넘어간 학생이 다리 없이 두 이름을 만난다.
대조군도 셋이다 — 통제변수 / 통제군·처치군 / 대조군.

- 표기 하나를 정하고(가이드 기준), 다른 이름은 첫 등장에 한 번만 병기
- **표기 규칙을 `docs/`에 남겨** 다음 사례가 또 갈리지 않게 한다

## N. 재검토 대상 (기각 아님)

### N1. S2가 0건이면 폴백하지 않는다 `[?]`

`searchChain.ts:61-66`은 S2가 200이면 0건이어도 즉시 반환하는데,
`:84`의 Crossref는 0건이면 OpenAlex로 넘어간다. 같은 "0건"에 다른 규칙이다.
`searchChain.test.ts:108`이 이걸 **의도된 동작으로 못 박아 뒀다.**

실제 결과: UI가 권하는 영어 키워드조차 S2 인덱스에 없으면 "검색 결과가 없습니다"로 끝난다 —
Crossref·OpenAlex에는 있는데도.

비대칭을 없애려면 S2 0건도 폴백하고 `source` 표시로 정직성을 유지하면 된다.
비용은 0건 검색 1건당 요청 2회 추가.

**의도된 설계를 뒤집는 것이라 운영자 판단으로 넘긴다.** 테스트가 명시적으로 잠가 둔 동작이다.

---

## 실행 순서 갱신 (라운드 1~3 통합)

**묶음 0을 신설한다. 다른 무엇보다 먼저다.**

| 묶음 | 항목 | 성격 |
| --- | --- | --- |
| **0. 치명 결함** | K1 K2 K3 + L1 L6 | 조용한 데이터 손실·오답. 서로 다른 파일이라 병렬 |
| **1. 앵커 연결** | A1 + G1 + 주석 정정 | 이미 만든 기능 연결 |
| **2. SEO 소품** | A2 A3 A4 A5 A7 B7 B8 | 리스크 없음 |
| **2.5. 검색 라우트** | L2 L3 | 한 파일, 예산 재계산 |
| **3. 모바일** | G6 G7 H6 H7 A6 | 375px 실측 |
| **4. /activity** | G3 G4 G5 + L4 | 한 파일군 |
| **4.5. 접근성 배선** | L5 M1 M2 M3 | 전부 S, 병렬 |
| **5. 통계** | H1 H2 B1 G2 | 테스트 먼저 |
| **5.5. 저장 계층** | L7 + C4 | 같은 훅 |
| **6. 도구 신설** | H3 H4 B2 B3 B4 | 독립 |
| **7. 링크·구조** | B5 B6 C3 C6 | |
| **7.5. 사례 편집** | M4 M6 M7 M8 | 한 번에 |
| **8. 교사** | I1 I2 | |
| **8.5. 용어·가독성** | M9 M10 | 표기 규칙 문서 먼저 |
| **9. 콘텐츠** | I4(3편) C5 M5 | 사실 검증 |
| **10. 큰 것** | C1 C2 I3 B9 | |

**묶음 0은 오늘 안에 나가야 한다.** K1은 사용자가 백업을 복원할 때마다 룰렛을 돌리는 상태다.

## 다음 라운드에 넘기는 메모

라운드 1~3에서 판정된 77건은 다시 올리지 말 것. 아직 안 본 영역:

- 실제 브라우저에서 도구 24개를 끝까지 조작해 보는 검증 (지금까지는 코드 읽기다)
- 다크모드에서만 나타나는 문제
- 첫 방문자의 실제 동선 — 홈에서 어디로 가고 어디서 막히나
- 경쟁 사이트가 주는데 우리가 안 주는 것
- 자료실 31편 상호 링크의 밀도와 고아 문서
- 애드센스 정책 관점의 페이지별 점검 (콘텐츠 대비 광고, 얇은 페이지)

---

# 라운드 4 (2026-09-05)

첫 방문 동선·링크 구조·다크모드·애드센스 정책·경쟁 비교·홈. 브레인스토밍 21건.
**이번 라운드의 수확은 다크모드다** — 라이트에서만 검증된 것이 세 겹으로 쌓여 있다.

## 자료실 31편 링크 집계 (실측)

`extractInternalLinks` 로직을 그대로 옮긴 스크립트로 셌다. **글→글 링크 71개, 깨진 링크 0개.**

| 구분 | 개수 | slug |
| --- | --- | --- |
| **고아 (inbound 0)** | 4 | `survey-questions` `why-topics-go-stale` `public-data-topics` `finding-calls` |
| **막다른 길 (outbound 0)** | 4 | `abstract-and-title` `presentation` `statistics` `research-ethics` |
| **허브 (inbound 상위)** | — | `research-ethics` 9 · `statistics` 7 · `question-vs-researchable` 5 · `reading-english-papers` 5 |
| **inbound ≤1** | 13/31 | 위 고아 4편 포함 |

**허브 상위 2편이 동시에 막다른 길이다.** 링크가 가장 많이 흘러드는 두 지점에 배출구가 없다.
`research-ethics`는 3,280자로 31편 중 가장 짧기까지 하다.

도구 딥링크: 자료실 본문의 `#tool-*` 링크 36개가 고유 도구 15개에 닿는다.
**24개 중 9개는 어느 글에서도 링크되지 않는다** —
`design-type` `purpose` `survey-bias` `random-sample` `phrases` `length` `ai-disclosure` `questions` `showcase`.

## O. 채택 — 높음 (라운드 4)

### O1. 다크모드에서 인용문 23편이 사실상 안 보인다 `[ ]` S

**확인**: `prose-invert`가 저장소 전체에 **0회**다.
네 곳(`articles/[slug]:125`, `guide/[stage]:109`, `showcase/[slug]:84`, `privacy:18`)이
`prose-headings/p/li/strong/a/th/td`만 덮어썼다.
따라서 `--tw-prose-quotes`·`-code`·`-pre-*`·`-hr`·`-bullets`·`-captions`는
라이트 기본값(neutral-900/200 계열)이 다크에서도 그대로 남는다.

blockquote는 31편 중 **23편**이 쓰고, `---` 구분선은 31편 **전부**가 쓴다.

- **`@theme`에서 `--tw-prose-*`를 토큰에 직접 바인딩한다.** 네 곳의 className 나열보다 견고하다
- 앞으로 새 prose 요소가 생겨도 안 깨진다 — 이게 후자를 고르는 이유

### O2. `color-scheme` 선언이 없어 다크에서 네이티브 컨트롤이 흰색이다 `[ ]` S

**확인**: `globals.css` 186줄에 `color-scheme`이 **없다**. 40줄의 `prefers-color-scheme` 미디어쿼리만 매치된다.

네이티브 컨트롤을 쓰는 컴포넌트가 11개다 —
`ChecklistCard`(체크박스, 6단계 전부), `DeadlineTracker.tsx:132`의 `type="date"`(달력 팝업이 통째로 흰색),
`VariableTableBuilder`·`RandomSampler`·`SampleSizeCalculator`의 `<select>`/`type="number"`.
스크롤바도 마찬가지다 — `globals.css:35-38`이 `.rail-scroll`에만 개별 처리한 것이 이 증상의 국소 대응이다.

- `:root`에 `color-scheme: light`, 다크 두 블록(`:41`, `:56`)에 `color-scheme: dark`
- `.rail-scroll` 개별 처리를 이걸로 대체할 수 있는지 확인한다

### O3. 차트 축·눈금·범례 색이 하드코딩돼 다크에서 사라진다 `[ ]` M

`SimpleChart.tsx`에 라이트 값 하드코딩이 **13곳**이다.
격자선 `#e6e7e9`(다크 `--line`은 `#2a2c2f`), 라벨·범례 `#17181a`(라이트 `--ink`),
축 제목 `#6b7076`(라이트 `--ink-soft`).

다크 배경 `#101113` 위에서 `#17181a` 텍스트는 **대비 1.05:1 — 읽을 수 없다.**
5단계 도구이고 `14-figure-first.mdx`가 이 도구를 전제로 그림 읽기를 가르친다.

- `currentColor` + 부모의 `text-ink`/`text-ink-soft`로 전환하거나 SVG 속성에 `var(--ink)`
- **난점**: `:84`의 `downloadSvg()`가 SVG를 파일로 내보낸다.
  내보낸 파일은 사이트 밖에서 열리므로 **다운로드 시점에는 라이트 고정 색을 인라인으로 박아야 한다.**
  화면용/내보내기용 색을 분리하는 것이 이 수정의 유일한 실제 난점이다 — 여기서 실수하면
  학생이 논문에 넣은 그림이 흰 배경에 흰 글씨가 된다

### O4. 데이터 계열 팔레트가 다크 대비를 검증받지 않았다 `[ ]` S

`SimpleChart.tsx:24-33`의 `COLORS` 8개는 라이트 기준 중명도 색이다.
`#2f6f4f`(라이트 `--accent`와 동일)는 다크 `#101113` 대비 약 2.6:1로
비텍스트 그래픽 최소 3:1에 미달한다. `#8b5fbf`·`#c24a6b`도 경계다.

`globals.css:47`이 다크에서 accent를 `#4fa377`로 올린 것과 **같은 판단이 차트 팔레트에는 적용되지 않았다.**

- 다크용 팔레트 8색을 병렬로 두고 `data-theme`로 고른다. O3과 같은 커밋

### O5. 404·`/activity`·`/guide/print`에 광고가 붙는다 `[ ]` M

**애드센스 재검토 중인 지금 가장 위험한 항목이다.**

`app/layout.tsx:128-133`의 좌우 레일이 레이아웃에 있어 **모든 라우트**에 렌더된다.
`hidden 2xl:block`이라 1536px 이상에서만 보이지만, 보이는 순간 정책 위반 소지다.

- 404: 본문 35줄, 실질 텍스트 3문장. 애드센스는 오류 페이지 게재를 금지한다
- `/activity`: 58줄, 서버 렌더 텍스트 4문단. 나머지는 전부 localStorage 의존이라
  첫 방문자에게는 **회색 격자 + 광고 두 개**가 전부다. G3이 채택돼 있어도 이건 별개 문제다
- `/guide/print`: 23줄, noindex

- 레일을 레이아웃에서 빼고 게재할 페이지에서 명시적으로 부른다 —
  단계·가이드 목록·자료실 상세·`/example`. 이쪽이 정직하다
- 또는 최소한 세 라우트에서 끄는 경로를 만든다

### O6. 홈에서 도구 24개로 가는 길이 푸터 12px 글자 하나뿐 `[ ]` S

**확인**: `app/page.tsx`에 `/tools` 링크가 **0개**다. 헤더도 3개뿐.
유일한 경로가 `SiteFooter.tsx`의 12px "도구" 글자다.

그런데 `app/tools/page.tsx:34`가 메타 타이틀로 "연구 도구 24개"를 내걸 만큼 이게 사이트의 차별점이다.

- 홈 6단계 섹션 각 행에 이미 `도구 N`이 표시된다. 그 숫자 옆이나 섹션 하단에 `/tools` 링크
- **E1(헤더 항목 논쟁)과 무관하게 홈만으로 해결된다** — 운영자 판단을 기다릴 필요가 없다

### O7. 검색으로 자료실 한 편에 떨어진 사람에게 도구가 안 보인다 `[ ]` M

`app/articles/[slug]/page.tsx:153-207`의 글 하단은 ① 이어지는 단계 ② 다른 문서 3편 ③ 이전/다음뿐이다.
`/tools`로도 홈으로도 가지 않는다. 상단 87줄의 "← 자료실"이 유일한 상승 경로다.

자료실 31편은 검색 유입이 가장 큰 표면인데, 그 착지점에서 사이트의 나머지가 보이지 않는다.

- 하단에 이 글의 `relatedStage` 도구 2~3개를 카드로
- `STAGE_TOOL_TITLES`와 `TOOL_IDS`가 이미 있어 데이터 추가가 필요 없다
- **A1·G1과 같은 메커니즘의 세 번째 발신처다.** 세 곳을 한 번에 처리하는 것을 검토한다

### O8. 허브 2편에 배출구 만들기 `[ ]` S

`research-ethics`(in 9 / out 0)와 `statistics`(in 7 / out 0).
`app/articles/[slug]/page.tsx:52`의 "다른 문서" 3편이 붙긴 하지만
본문 맥락과 무관한 카테고리 순서 목록이다.

- `research-ethics` → `limitations` `lab-notebook` `/guide/data-collection`
- `statistics` → `small-sample` `negative-results` `figure-first`
- 본문 안 자연스러운 위치에 인라인 우선, 없으면 말미 절
- `abstract-and-title`·`presentation`도 같이

## P. 채택 — 보통 (라운드 4)

### P1. 고아 4편 해소 — 특히 `survey-questions` `[ ]` S

`02-survey-questions.mdx`는 inbound 0인데,
이 글의 주제와 1:1로 대응하는 도구 `설문 문항 편향 체크`(id `survey-bias`)로 가는 링크가 **0개**다.
반대로 `SurveyBiasChecker`에서 이 글로 오는 링크도 없다. 양방향으로 끊겨 있다.

- 본문에서 `/guide/topic#tool-survey-bias`로 보낸다
- `research-ethics`·`question-to-test`·`03-methodology`에서 이 글을 인용해 고아를 푼다
- 나머지 3편(`why-topics-go-stale` `public-data-topics` `finding-calls`)도 같은 방식

### P2. 링크 없는 도구 9개에 진입로 `[ ]` M

B5(도구→글)의 **반대 방향**이다. 9개 각각에 대해 가장 가까운 글 한 편에 딥링크 한 줄.

특히 `ai-disclosure`(AI 활용 명시 문구)는 2026년 현재 가장 검색되는 주제인데
31편 어디에도 링크가 없다. `phrases`도 `05-reading-english-papers.mdx`가 있는데 연결이 없다.

- 대응할 글이 없는 도구는 그 사실을 **콘텐츠 공백으로 기록**한다 — 억지로 붙이지 않는다
- 검증은 `articles.test.ts`의 앵커 검증 패턴 재사용

### P3. "다른 문서" 3편이 카테고리 안에서 전부 같다 `[ ]` S

`app/articles/[slug]/page.tsx:46-52`가 `sameCategory`를 필터 순서 그대로 `slice(0,3)`.
`search` 카테고리 8편은 4~8번째 글이 **모두 같은 세 편만** 추천한다.

44줄 주석은 "모든 글이 같은 3편을 추천하게 된다"를 막으려 했다고 적었는데,
**카테고리 안에서 그 문제가 그대로 재현된다.**

- slug 해시로 카테고리 내 회전 오프셋, 또는 `keywords` 교집합 크기로 정렬
- **결정적이어야 한다.** 난수 금지 — 빌드마다 바뀌면 캐시·테스트가 무너진다

### P4. `/showcase`가 앱 코드 어디에서도 링크되지 않는다 `[ ]` S

앱 코드에서 `/showcase`를 링크하는 곳은 상세→목록 하나뿐이다.
`SiteFooter`는 도구·자료실·소개·개인정보·가이드 5개를 걸면서 사례를 뺐다.
유입은 자료실 본문 인라인 8개뿐인데 `sitemap.ts:72`가 이 URL 7개를 priority 0.7로 제출한다.

**사이트 내부 링크가 사실상 없는 URL을 색인 요청하고 있다.**

- 푸터에 "연구 사례" 한 항목
- E2(홈 사례 섹션)는 운영자 판단 대기지만 **푸터는 별개 판정이다** — 홈을 건드리지 않는다

### P5. `/example`이 최장 페이지인데 광고가 0개 `[ ]` S

382줄로 단일 라우트 중 최장인데 `AdSlot` import가 없다.
1536px 미만에서는 레일도 안 뜨므로 **모든 모바일에서 수익 면적 0**이다.
홈에서 직접 링크되는 두 CTA 중 하나이고 체류시간이 가장 길 페이지다.

A4가 자료실 상세만 다뤘고 `/example`은 판정된 적이 없다.

- 하단 6단계 종료 직후 `2xl:hidden` `AdSlot` 1개. 본문 중간 삽입은 A4와 같은 이유로 안 한다

### P6. `/showcase`·`/tools` 목록의 두께 `[ ]` S

- `/showcase` 61줄: 안내 2문단 + 6개 링크. `:16-18` 주석이 "빈 목록은 얇은 콘텐츠"라며
  0건일 때 `notFound()`를 걸었는데 **6건일 때의 얇음은 다루지 않았다**
- `/tools` 118줄 중 실질 텍스트는 안내 2문단 + 단계 소개 6줄 + 도구 설명 24줄. 나머지는 링크다.
  A1이 링크 정확성을 고치지만 **분량은 그대로**다

- `/showcase`: "어떤 사례를 싣나 / 어떻게 읽으면 좋나" + 항목마다 한 줄 배움점
- `/tools`: 각 단계에 "이 단계 도구를 언제 쓰나" 3~4문장.
  `STAGE_TOOL_INTROS`가 이미 한 문장씩 있으니 확장이지 창작이 아니다
- **P5와 함께 "애드센스 두께" 묶음으로.** O5와 같은 커밋

### P7. `featured` 10편 중 4편이 영영 안 나온다 `[ ]` S

`featured: true`가 **10편**인데 `app/page.tsx:76`이 `getFeaturedArticles(6)`을 부르고
`lib/articles.ts:143`이 `slice(0, limit)`으로 자른다.
`figure-first` `from-curriculum` `question-to-test` `negative-results` 4편은
플래그를 켜 놓고도 노출되지 않는다.

**운영자가 플래그를 켠 의도가 조용히 사라진다.**

- 6편 유지면 나머지 4편 플래그를 끈다. 아니면 한도를 10으로 올린다
- 어느 쪽이든 "featured인데 안 보이는 상태"를 없앤다
- 테스트: `featured` 개수와 홈 한도가 어긋나면 실패하게 한다

### P8. 홈에 "무엇을 얻는가"의 증거가 없다 `[ ]` M

`app/page.tsx` 히어로부터 클로징까지 전부 텍스트 목록이다.
24개 도구·31편 자료실 중 **화면으로 보여주는 것이 하나도 없다.**
히어로의 유일한 CTA "6단계 가이드 보기"를 누르면 도착하는 `/guide`가 또 목록이다.

첫 화면에서 도구를 본 적 없는 사람이 **두 번의 목록을 넘어야** 도구에 닿는다.

- 홈에 도구 하나를 실제 조작 가능한 상태로 임베드. `SurveyBiasChecker`나 `SampleSizeCalculator`
- 전부 `next/dynamic` 지연 로딩이라 번들 부담이 작다
- **O6과 다른 층위다.** O6은 링크, 이건 증거

## Q. 조건부 채택 (라운드 4)

### Q1. 사전등록 간이 템플릿 `[ ]` M

OSF가 13종 이상의 사전등록 템플릿을 무로그인으로 공개한다.
우리는 H4가 "이상치 기준을 수집 전에 적는다"를 UI로 강제하려 하고
`03-methodology.mdx`가 가설·측정·표본을 먼저 정하라고 가르치는데,
**그걸 한 장으로 묶어 먼저 적게 하는 산출물이 없다.**

`29-negative-results.mdx`가 다루는 문제(결과 보고 가설 바꾸기)의 유일한 구조적 해법이다.

- 3단계 도구. 질문·가설·측정 방법·표본 크기·분석 방법·중단 기준 + 마크다운 내보내기
- `VariableTableBuilder` 골격 재사용, `SampleSizeCalculator` 결과와 연결
- **H1·H4 이후에 착수한다.** 그 둘의 산출물을 받아 쓰는 도구다

### Q2. 대회 규정·부문 비교표 `[ ]` M

저장소 전체에 `ISEF` 문자열이 **0회**다.
`31-finding-calls.mdx:26`이 교육청 공고 찾는 법만 다루고,
어느 대회가 어떤 부문·자격·보고서 규격을 요구하는지 정리한 곳이 없다.

ISEF는 부문 22개와 필수 서식, Rules Wizard, 카테고리 PDF, 생성형 AI 사용 허용 범위표까지 무료로 공개한다.
국내에 대체재가 없다.

- 정적 비교표 1편 + "내 주제는 어느 부문인가" 대응표(전람회 부문 ↔ ISEF 22 카테고리)
- **원본 서식·룰북은 재배포하지 않는다.** 한국어 해설 + 원문 직링크만
- C5와 인접하지만 축이 다르다 — C5는 "어디에 내나", 이건 "무엇을 갖춰야 내나"
- 규정은 해마다 바뀐다. **"2026년 기준" 명시와 원문 확인 안내를 반드시 붙인다**

### Q3. 전국과학전람회 수상작 검색기 `[?]` L

주제 선정에서 학생이 가장 알고 싶은 것은 "선배들이 뭘 했나"인데,
`TopicIdeaGenerator`(`lib/ideaBank.ts` 162줄)는 무작위 조합 프롬프트를 던질 뿐 실제 사례 코퍼스가 없다.

공공데이터포털에 국립중앙과학관 수상작정보 약 970건이 파일데이터로 공개돼 있다.

- CSV를 빌드 타임 정적 JSON으로 번들해 부문·학교급·연도·키워드로 거르는 클라이언트 검색기
- 백엔드 불필요, 무로그인 원칙 준수
- **선행 확인 필수**: 공공누리 라이선스의 재배포 허용 여부.
  이걸 확인하기 전에는 착수하지 않는다 — **운영자 판단으로 넘긴다**
- 데이터에 지도교사 실명이 포함돼 있다. 재배포하더라도 **개인명 열은 제외한다**

## R. 기각 (라운드 4)

### R1. Zooniverse 시민과학 프로젝트 안내 `[-]`
**기각 사유**: 프로젝트 상태가 계속 바뀌어 낡으면 해악이다.
C5에서 "분기별 갱신 캘린더"를 거부한 것과 같은 판단.

### R2. 크레존·에듀넷 자료 재활용 `[-]`
**기각 사유**: 두 곳 모두 로그인 벽이 있어 콘텐츠를 가져올 수 없다. 링크 안내만 가능한데 실익이 작다.

### R3. 주제 아이디어 DB 500건 큐레이션 `[-]`
**기각 사유**: 사실 검증·저작권 부담이 크다.
Q3(공공데이터 수상작)이 같은 수요를 라이선스가 명확한 경로로 충족한다. 그쪽이 먼저다.

---

## 실행 순서 갱신 (라운드 1~4)

| 묶음 | 항목 | 성격 |
| --- | --- | --- |
| **0. 치명 결함** | K1 K2 K3 L1 L6 | 조용한 손실·오답 |
| **0.5. 애드센스 방어** | O5 P5 P6 | 재검토 중이라 시급 |
| **1. 앵커 연결** | A1 G1 O7 + 주석 정정 | 같은 메커니즘 세 발신처 |
| **1.5. 다크모드** | O1 O2 O3 O4 | 라이트에서만 검증된 것들 |
| **2. SEO 소품** | A2 A3 A4 A5 A7 B7 B8 P7 | 리스크 없음 |
| **2.5. 검색 라우트** | L2 L3 | 예산 재계산 |
| **3. 모바일** | G6 G7 H6 H7 A6 | 375px 실측 |
| **4. /activity** | G3 G4 G5 L4 | |
| **4.5. 접근성 배선** | L5 M1 M2 M3 | 전부 S |
| **5. 통계** | H1 H2 B1 G2 | 테스트 먼저 |
| **5.5. 저장 계층** | L7 C4 | 같은 훅 |
| **6. 도구 신설** | H3 H4 B2 B3 B4 Q1 | |
| **7. 링크 구조** | O6 O8 P1 P2 P3 P4 B5 B6 C3 C6 | 라운드 4 집계가 대상을 확정했다 |
| **7.5. 사례 편집** | M4 M6 M7 M8 | |
| **8. 교사·홈** | I1 I2 P8 | |
| **8.5. 용어·가독성** | M9 M10 | 표기 규칙 먼저 |
| **9. 콘텐츠** | I4 C5 M5 Q2 | 사실 검증 |
| **10. 큰 것** | C1 C2 I3 B9 Q3 | |

**묶음 0.5를 신설했다.** 애드센스 재검토 중에 404와 빈 `/activity`에 광고가 붙는 것은
기능 개선이 아니라 방어다. 묶음 0 바로 다음이다.

## 다음 라운드에 넘기는 메모

라운드 1~4에서 판정된 98건은 다시 올리지 말 것. 아직 안 본 영역:

- 실제 브라우저 조작 검증 (지금까지 전부 코드 읽기다) — 특히 다크모드 실물 확인
- 테스트 스위트 자체의 빈 구멍 — 202개가 무엇을 안 보고 있나
- `content/guide/*.mdx` 6편의 frontmatter 정합성과 체크리스트 항목 품질
- 오래된 사실 — 외부 사이트 UI 설명, 링크 부패, "2026년 기준" 표기가 필요한 곳
- 운영 자동화 — 색인 요청, 사이트맵 제출, 배포 후 점검을 사람이 매번 하고 있나
- 성장 전략 — 지금 구조에서 글 100편이 되면 무엇이 먼저 무너지나
