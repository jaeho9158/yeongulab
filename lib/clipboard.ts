/**
 * 복사 실패 시 보여줄 공용 안내 문구.
 *
 * catch를 비워두면 버튼이 먹통처럼 보이고, 사용자는 복사된 줄 알고 이전
 * 클립보드 내용을 붙여넣는다(인용 형식에서 특히 위험하다). 그래서 실패를
 * 반드시 화면에 남기고, 대안(직접 선택해 복사)까지 알려준다.
 *
 * 도구마다 다른 말로 알리면 같은 사고가 다른 문제처럼 보이므로 문구는 한 곳에
 * 둔다.
 */
export const COPY_FAILED_MESSAGE =
  "복사하지 못했습니다. 위 내용을 직접 선택해 복사해주세요.";
